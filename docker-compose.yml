services:
  db:
    image: postgres:15-alpine
    container_name: vpn_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-vpnbot}
    ports:
      - "${DB_EXTERNAL_PORT:-5432}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - vpn_net

  app:
    build: .
    container_name: vpn_app
    restart: unless-stopped
    env_file: .env
    environment:
      SERVER_HOST: "0.0.0.0"
      SERVER_PORT: "8000"
    volumes:
      - app_logs:/app/logs
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - vpn_net

  frontend:
    build: ./frontend
    container_name: vpn_frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      app:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--spider", "http://localhost:80/"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - vpn_net

  nginx:
    image: nginx:alpine
    container_name: vpn_nginx
    restart: unless-stopped
    env_file: .env
    ports:
      - "80:80"
      - "8443:8443"
    volumes:
      - ./nginx/nginx.local.template.conf:/etc/nginx/templates/nginx.local.template.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl
      - ./nginx/entrypoint.sh:/docker-entrypoint.d/01-generate-ssl.sh:ro
      - ./app/static:/app/app/static:ro
    depends_on:
      app:
        condition: service_healthy
    networks:
      - vpn_net

networks:
  vpn_net:
    driver: bridge

volumes:
  db_data:
  app_logs:
